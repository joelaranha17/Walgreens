<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.firstapp.joel.walgreens">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/walgreens"
        android:label="Walgreens"
        android:roundIcon="@mipmap/walgreens"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".util.one.SplashScreen"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".util.one.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".util.Login.SecondActivity" />
        <activity android:name=".util.three.Third" />
        <activity android:name=".util.four.Fourth" />
        <activity android:name=".util.five.Fifth"></activity>
    </application>

</manifest>